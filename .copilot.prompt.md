Update `sycript copilot` command:
1. Always transfer the `.copilot.prompt.md` file to the remote server before executing the copilot command, ensuring that the prompt is available for the copilot command to read and use during execution.
