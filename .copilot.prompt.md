Update `copilot` command on server:
1. Add `--resume <uuid-sessio-id>` to match the generated session ID and resume streaming the log file from the last position.
2. Add `--autopilot` 
3. Add current folder name to the log file name to differentiate between different copilot sessions running in different directories. For example, `copilot-{current-folder-name}-{copilot-session-id}.log`.
4. Add a timestamp to the log file name for better tracking, e.g., `copilot-{current-folder-name}-{copilot-session-id}-{timestamp}.log`.
5. A bug: Listing logs doesn't work correctly: 
   ```
PS C:\Users\bs\projects\personal\syncript> syncript copilot logs
[16:14:32] [SSH] connecting to root@136.0.10.24:9011 …
[16:14:35] [SSH] connected ✓
[16:14:36] [SSH] disconnected.
SESSION ID                              MODIFIED                 SIZE
------------------------------------------------------------------------
   ```
